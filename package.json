{
  "name": "nullbrowser",
  "version": "1.1.0",
  "description": "Mini browser with maximum adblocking for Android",
  "scripts": {
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0",
    "@capacitor/browser": "^5.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
